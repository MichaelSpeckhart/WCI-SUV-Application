<Window x:Class="WCI_SUV_UI.Views.AddOpcConfigurationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WCI_SUV_UI.Views"
        mc:Ignorable="d"
        Title="AddOpcConfigurationView" Height="450" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Input Fields -->
        <StackPanel Grid.Row="0" Margin="5">

            <!-- Server Name -->
            <Grid Margin="0,5">
                <TextBox x:Name="ServerNameTextBox" Margin="0,5" Text="" VerticalContentAlignment="Center" TextChanged="ServerNameTextBox_Changed"/>
                <TextBlock x:Name="ServerNameWatermark" Text="Server Name" Foreground="Gray" FontStyle="Italic" VerticalAlignment="Center" IsHitTestVisible="False" Margin="5,0"/>
            </Grid>

            <!-- Endpoint URL -->
            <Grid Margin="0,5">
                <TextBox x:Name="EndpointTextBox" Margin="0,5" VerticalContentAlignment="Center" TextChanged="EndpointTextBox_Changed"/>
                <TextBlock x:Name="EndpointWatermark" Text="Endpoint URL" Foreground="Gray" FontStyle="Italic" VerticalAlignment="Center" IsHitTestVisible="False" Margin="5,0"/>
            </Grid>

            <!-- Username -->
            <Grid Margin="0,5">
                <TextBox x:Name="UsernameTextBox" Margin="0,5" VerticalContentAlignment="Center" TextChanged="UsernameTextBox_Changed"/>
                <TextBlock x:Name="UsernameWatermark" Text="Username" Foreground="Gray" FontStyle="Italic" VerticalAlignment="Center" IsHitTestVisible="False" Margin="5,0"/>
            </Grid>

            <!-- Password -->
            <PasswordBox x:Name="PasswordBox" Margin="0,5"/>
        </StackPanel>

        <!-- List of Configured Connections -->
        <ListBox x:Name="ConnectionsListBox" Grid.Row="1" Margin="0,10" Height="200">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding serverName}" Margin="1,0" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>


        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add" Click="AddConnection_Click" Margin="5"/>
            <Button Content="Remove" Click="RemoveConnection_Click" Margin="5"/>
            <Button Content="Save" Click="SaveConnections_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
